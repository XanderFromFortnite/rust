import os
import socket
import signal

os.system('clear')
ipt = "Your ip address is: "

print (ipt)
(os.system('ip route get 2.4.6.8'))

input("Press Enter to continue...")
import start

signal.pause()
